# Helix verification record

This record reports commands actually run in the local workspace. It does not claim unperformed distributed, provider, or production-security validation.

| Check | Result | Evidence |
|---|---|---|
| Typecheck | Pass | `pnpm exec tsc -p tsconfig.json --noEmit` |
| Build | Pass | `pnpm exec tsc -p tsconfig.json` |
| Unit/integration tests | Pass, 115 tests | M1–M10 regression coverage plus M11/M12 MCP and autonomous-intelligence coverage; the M12 suite contains 20 deterministic scenarios for goal analysis, planning, validation, selection, execution, failure recovery, authorization, memory learning, explainability, metrics, restart recovery, and 100-agent safety |
| CLI smoke test | Pass | Compiled `helix run ... --json`, lifecycle controls, `helix recover --json`, M8 sandbox commands, M10 `memory stats/compact`, `learning flush`, and M11 `mcp doctor/tools/resources/prompts`; Docker availability is reported by `helix sandbox doctor` |
| API smoke test | Pass | Public health, unauthorized rejection, authenticated execution creation, checkpointing, legacy and typed memory write/search/get/delete, M10 memory compaction/cache metrics, learning hints/experience/outcome/flush, telemetry endpoints, local provider-wiring path, sandbox list/status/destroy routes, and M11 MCP launcher/official SDK surface are covered by the versioned API/runtime implementation |
| Dependency audit | Pass | `pnpm audit --audit-level high` reported no known vulnerabilities |
| Secret scan | Pass | Source-level scan found no private-key or common API-key patterns |
| Measured benchmark | Pass | 10 deterministic executions with durable lease persistence and sandbox-manager initialization: average 32.40 ms, min 18.54 ms, max 46.01 ms, 30.86 executions/s, 4 tasks and 19 events per execution |
| M8 sandbox demo | Pass | Local sandbox demo executes a real command through `SandboxManager`, prints resource/limitation metadata, and records secret-safe audit data; Docker unit policy tests pass without requiring a daemon |
| M9 memory-learning demo | Pass | `pnpm memory-demo` ran 100 agents and 1,000 deterministic tasks with no LLM calls, producing 3,000 memory entries and reporting initial/learned routing, success rate, useful patterns, improved agents, repeated-failure agents, and routing changes |
| M9 benchmark | Historical pass | Earlier M9 local deterministic result retained for comparison; it is not re-used as an M10 claim |
| M10 benchmark | Pass | 100 agents, 1,000 tasks, 10,000 seeded memories per backend. JSONL versus SQLite: lookup average 537.045086 ms vs 1.073750 ms; lookup p50 535.519977 ms vs 0.296387 ms; lookup p95 562.317838 ms vs 0.311631 ms; lookup p99 564.074295 ms vs 0.341481 ms; routing average 16.989621 ms vs 11.978535 ms; write average 3.766714 ms vs 14.759598 ms; throughput 20.998389 vs 37.301916 tasks/s; completion 0.909 vs 0.909; routing success 1.000 vs 1.000; final memory count 13,000 each. CPU user 55,129.853 ms vs 18,319.473 ms; CPU system 406.720 ms vs 8,429.667 ms; heap delta +124.500 MB vs +8.482 MB |
| M12 intelligence benchmark | Pass | 100 registered agents and 1,000 capability-safe task-unit selections. Analysis/planning/validation/execution/evaluation/replanning measured at 0.3587/0.5302/0.5001/42.7202/0.7029/0.6264 ms; selection average/p50/p95/p99 0.3144/0.2808/0.4130/0.8813 ms; SQLite memory lookup average/p50/p95/p99 0.0412/0.0381/0.0611/0.0805 ms; throughput 2,803.10 task units/s; full orchestration completed with score 1.0 and zero replans; heap delta -3.447 MB; CPU user/system 423.463/11.731 ms; no external calls |
| M12 intelligence demo | Pass | `pnpm intelligence:demo` ran 100 agents across a software pipeline, an authorized high-risk security plan, and a parallel analysis plan. The forced implementation failure triggered one bounded replan with an alternative agent; both executed plans completed with measured score 0.925 and sanitized durable events. |

The local deterministic provider reports zero tokens and zero cost because it performs no external model call. Benchmark figures are local measurements from this development machine, not production capacity guarantees. M10 materially reduced measured lookup and routing latency in its workload but increased synchronous write latency; task completion remained unchanged, so no learning-quality improvement is claimed. M11 measurements cover registry and MCP tool-call overhead, not 1,000 full multi-agent executions. SQLite shifts work into native/system CPU, and the asynchronous runtime learning queue is an in-process durability optimization rather than a distributed delivery guarantee. The API and MCP HTTP launcher bind to loopback by default; when `HELIX_API_KEY` is configured, API non-health routes require bearer authentication, and request bodies plus per-client request rates are bounded. CI not run; local verification passed.

## Release gates still required

A production release still needs authenticated API/MCP deployment, TLS termination, actor identity propagation, secret-manager integration, durable/shared MCP audit and rate-limit state, SQLite backup/restore and filesystem-permission review, distributed outbox design if learning durability must survive process loss, Docker daemon/image provenance review, seccomp/AppArmor policy review, signed images, sandbox chaos tests, MCP isolation tests with external clients, federation authentication tests, chaos tests against real worker processes, OpenTelemetry exporters, remote database/vector adapters, and human review of policy definitions. Docker integration was explicitly skipped in this environment because no Docker daemon is available; Docker policy generation and availability detection were tested instead. These are explicit capability boundaries in [`capability-boundaries.md`](capability-boundaries.md), not hidden stubs.
