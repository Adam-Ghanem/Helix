# Helix Workers

Agent workers bridge the scheduler to executable task runtimes. The default `SimulatedExecutor` is deterministic and requires no LLM provider.

Public API: `AgentWorker`, `WorkerPool`, `SimulatedExecutor`, `TaskExecutor`, and worker lifecycle types.
